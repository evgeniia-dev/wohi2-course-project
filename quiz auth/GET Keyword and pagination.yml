info:
  name: GET Keyword and pagination
  type: http

http:
  method: GET
  url: http://localhost:3000/api/questions?keyword=html&page=1&limit=5
  params:
    - name: keyword
      value: html
      type: query
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
